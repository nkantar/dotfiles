git:
  pagers:
    - pager: delta --dark --paging=never
      colorArg: always

customCommands:
  - key: "S"
    context: "localBranches"
    command: "git sweep"
  - key: "F"
    context: "files"
    command: "git fire"
