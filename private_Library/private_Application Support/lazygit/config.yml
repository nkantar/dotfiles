git:
  paging:
    colorArg: always
    pager: delta --dark --paging=never

customCommands:
  - key: "S"
    context: "localBranches"
    command: "git sweep"
  - key: "F"
    context: "files"
    command: "git fire"
