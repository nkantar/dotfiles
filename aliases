# ls
alias ls="ls -G"
alias cls="clear && ls"
alias lat="ls -lat"
alias clat="clear && ls -lat"
alias ll="ls -l"
alias cll="cls && ls -l"

# git
alias gprune='git branch --merged | grep -v "\*" | grep -v "master" | xargs -n 1 git branch -d'

# nvim
alias nv='nvim'

