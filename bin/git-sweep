#!/usr/bin/env bash

# Delete any local branches that have been merged into master.

set -e

echo "Deleting all branches merged into master..."
git branch --merged | grep -v "\*" | grep -v "master" | xargs -n 1 git branch -d

